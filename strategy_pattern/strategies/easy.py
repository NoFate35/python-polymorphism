class Easy():
    # BEGIN (write your solution here)
    def step(self, field) -> None:
        def analyze_field(field):
            for raw in field:
                
                print('rawww', raw)
        raw, col = analyze_field(field)
        
    # END
